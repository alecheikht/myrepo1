/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListeslidesComponent } from './listeslides.component';

describe('ListeslidesComponent', () => {
  let component: ListeslidesComponent;
  let fixture: ComponentFixture<ListeslidesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListeslidesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListeslidesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/