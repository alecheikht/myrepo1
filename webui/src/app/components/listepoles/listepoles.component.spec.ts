/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListepolesComponent } from './listepoles.component';

describe('ListepolesComponent', () => {
  let component: ListepolesComponent;
  let fixture: ComponentFixture<ListepolesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListepolesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListepolesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/