/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ModalValiderActionComponent } from './modal-valider-action.component';

describe('ModalValiderActionComponent', () => {
  let component: ModalValiderActionComponent;
  let fixture: ComponentFixture<ModalValiderActionComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ModalValiderActionComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ModalValiderActionComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/